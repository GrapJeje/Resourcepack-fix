name: ResourcepackFix
version: '${project.version}'
main: eu.camonetwork.resourcepackfix.Main
api-version: '1.15'

commands:
  resourcepackfix:
    description: Main command
    default: yes
    aliases:
      - rpf
      - rpfix
      - texturepackfix
      - tpf
      - tpfix